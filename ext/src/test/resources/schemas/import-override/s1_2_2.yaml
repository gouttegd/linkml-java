id: https://example.org/import-override/s1_2_2
name: import-override-s1_2_2

prefixes:
  linkml: https://w3id.org/linkml/

imports:
  - linkml:types

slots:

  Eve:
    description: Eve from S1-2-2.
    notes:
      - Should be overridden by S2-1, which is imported after S1 and all its imports.

  Fiona:
    description: Fiona from S1-2-2.
    notes:
      - Should be overridden by S1-2, which comes earlier in the same import chain.
